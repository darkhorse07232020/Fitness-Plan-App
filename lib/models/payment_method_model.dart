class PaymentMethodModel {
  final int id;
  final String title;
  final String subtitle;
  final String icon;
  bool isSelected;

  PaymentMethodModel({this.id, this.title, this.subtitle, this.icon,this.isSelected});
}
